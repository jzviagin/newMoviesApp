import React from 'react';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  image: {
    width: '100%',
    height: 400,
    marginVertical: 20,
  },
  bigText: { fontSize: 17, color: 'black', marginHorizontal: 10 },
  smallText: { fontSize: 14, marginHorizontal: 10 },
});
